# Core dependencies for Sage MCP Single Container (Cloud API Version)
# Uses SiliconFlow API instead of local ML models

# MCP SDK
mcp>=1.1.0

# Database
asyncpg>=0.29.0
pgvector>=0.2.5

# HTTP client for API calls
requests>=2.31.0

# Core
numpy>=1.24.0  # For vector operations

# Authentication
PyJWT>=2.8.0

# Web server (optional, not used in STDIO mode)
# fastapi>=0.104.0
# uvicorn>=0.24.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1